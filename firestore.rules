rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for posts, pages, theme settings, and messages
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
